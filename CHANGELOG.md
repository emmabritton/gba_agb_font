# v0.25.0

No changes, but for simplicity as the renderer is tied to AGBs version, these libraries will match AGBs version number

# v0.24.0

No changes, but for simplicity as the renderer is tied to AGBs version, these libraries will match AGBs version number

# v0.6.0

- Change how word wrapping is handled internally
- Add some tests
- Add more debug checks

# v0.5.0

- Initial version